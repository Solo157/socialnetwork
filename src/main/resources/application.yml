spring:
  application:
    name: socialnetwork

  datasource:
    url: jdbc:postgresql://db:5432/social_network
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always
